# 🚀 快速部署到 GitHub

## 最简单的方法（3步完成）

### 第1步：获取 GitHub Token
1. 访问 https://github.com/settings/tokens/new
2. 填写 Note：Shell Utils Deploy
3. 勾选 `repo` 权限（完整仓库访问）
4. 点击 Generate token
5. **复制保存好这个 token**（只显示一次）

### 第2步：设置环境变量并部署

在终端中运行以下命令（替换为您的实际信息）：

```bash
cd /workspace

# 设置您的信息
export GH_TOKEN="ghp_您的token_here"
export GH_USERNAME="您的GitHub用户名"
export GH_REPO_NAME="shell-utils"      # 可选
export GH_VISIBILITY="public"          # 可选: public 或 private

# 执行部署
./deploy-automated.sh
```

### 第3步：完成！
脚本会自动创建仓库并推送代码，完成后会显示您的仓库地址！

---

## 备选方法：使用交互式脚本

如果您更喜欢问答式的部署方式，也可以运行：
```bash
./deploy-to-github.sh
```

---

## 验证部署

部署成功后，访问：
```
https://github.com/您的用户名/shell-utils
```

您应该能看到所有文件！🎉
